const $=s=>document.querySelector(s);document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('.titlebar nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));$('#'+b.dataset.tab).classList.add('active')});
const code=$('#code'),preview=$('#preview'),consoleEl=$('#console');function run(){const src=code.value;preview.innerHTML='';const bg=src.match(/ui background\s+["'](.+?)["']/i);if(bg)preview.style.background=bg[1];const win=src.match(/ui window\s+["'](.+?)["']/i);if(win){const h=document.createElement('h2');h.textContent=win[1];preview.appendChild(h)}const texts=[...src.matchAll(/ui text\s+["'](.+?)["']/gi)];texts.forEach(m=>{const p=document.createElement('p');p.textContent=m[1];preview.appendChild(p)});const buttons=[...src.matchAll(/ui button\s+["'](.+?)["']/gi)];buttons.forEach(m=>{const b=document.createElement('button');b.textContent=m[1];b.onclick=()=>consoleEl.textContent+=`\n${m[1]} clicked`;preview.appendChild(b)});const says=[...src.matchAll(/say\s+["'](.+?)["']/gi)];consoleEl.textContent=says.map(x=>x[1]).join('\n')||'Flux visual preview ready.'}$('#run').onclick=run;run();
$('#newProject').onclick=()=>{code.value='ui window "My Flux Project"\nui background "#0a0d14"\nui text "Welcome to Flux."\nui button "Start"\n    colour "#8b5cf6"\ndone\n';document.querySelector('[data-tab="playground"]').click();run()};
$('.close').onclick=()=>window.close();$('.min').onclick=()=>window.fluxStudio?.minimize?.();$('.max').onclick=()=>window.fluxStudio?.maximize?.();